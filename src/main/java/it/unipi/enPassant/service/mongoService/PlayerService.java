package it.unipi.enPassant.service.mongoService;
import org.springframework.stereotype.Service;

@Service
public class PlayerService {
    public boolean enrollCategory(String username, String category) {
        /*here we have to add a player to a bucket with all the
        added player so at the end of the day we pass to mongoDB*/
        return true;
    }
    public boolean insertMove(String matchid, String username, String move){
        /*here we have to add a player to a bucket with all the
        added player so at the end of the day we pass to mongoDB*/
        return true;
    }
}
